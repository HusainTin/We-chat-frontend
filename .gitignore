# Node.js
node_modules/

# Build output
.next/
out/

# Local environment files
.env
.env.local
.env.development
.env.test
.env.production

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# VS Code settings
.vscode/

# macOS system files
.DS_Store

# Windows system files
Thumbs.db

# Next.js typescript build (if using TypeScript)
*.tsbuildinfo

# npm package lock file (optional: depending on your workflow, you might want to track it)
# package-lock.json

# Yarn lock file (optional: depending on your workflow, you might want to track it)
# yarn.lock

# Jest/Testing files (if applicable)
coverage/
*.test.js
*.test.ts
*.test.tsx

# Docker
docker-compose.override.yml
Dockerfile

# Next.js specific
# Ignoring build directory when not using "next export"
.next/  

# Temporary files for deployments (when using Vercel or Netlify)
.vercel/
.netlify/

# Optional: Ignore folders with sensitive data (e.g. .secrets)
.secrets/
node_modules
