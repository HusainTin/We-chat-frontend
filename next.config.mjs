/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['cdn.jsdelivr.net', 'user-images.githubusercontent.com','127.0.0.1'],
      },
    
};

export default nextConfig;
